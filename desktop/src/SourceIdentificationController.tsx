// Source identification can contact AcoustID/MusicBrainz and may change score metadata.
// Keep that enrichment behind an explicit user action instead of polling every song in
// the background. The Enrichment workspace owns user-initiated external metadata work.
export default function SourceIdentificationController() {
  return null;
}
